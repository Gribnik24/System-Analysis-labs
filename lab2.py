import streamlit as st
import pandas as pd
import numpy as np

st.set_page_config(page_title="Метод взвешенных экспертных оценок", layout="wide")

st.title("🧪 Метод взвешенных экспертных оценок")
st.header("Лабораторная работа 2, задание 9")

st.markdown("""
### Исходные данные для задания 9:

**Альтернативы:**
1. Строительство специализированного медицинского центра
2. Покупка дорогостоящего современного оборудования  
3. Строительство наркологического центра
4. Увеличение парка машин скорой помощи

**Компетентность экспертов:** R₁ = 8, R₂ = 6, R₃ = 7

**Матрица оценок:**
| Эксперт | Z₁ | Z₂ | Z₃ | Z₄ |
|---------|----|----|----|----|
| Э₁      | 0.3 | 0.37 | 0.23 | 0.1 |
| Э₂      | 0.15 | 0.35 | 0.23 | 0.27 |
| Э₃      | 0.15 | 0.15 | 0.35 | 0.35 |
""")

# Данные из задания
alternatives = [
    "Строительство специализированного медицинского центра",
    "Покупка дорогостоящего современного оборудования",
    "Строительство наркологического центра",
    "Увеличение парка машин скорой помощи"
]

expert_competency = [8, 6, 7]  # R1, R2, R3

# Матрица оценок (3 эксперта × 4 альтернативы)
P = np.array([
    [0.3, 0.37, 0.23, 0.1],
    [0.15, 0.35, 0.23, 0.27],
    [0.15, 0.15, 0.35, 0.35]
])

# Проверка корректности матрицы
if P.shape != (3, 4):
    st.error("Ошибка в размерах матрицы оценок!")
else:
    # 1. Нормируем компетентность экспертов
    S = np.array(expert_competency) / sum(expert_competency)

    # 2. Вычисляем веса альтернатив
    V = np.zeros(4)
    for j in range(4):  # по каждой альтернативе
        for i in range(3):  # по каждому эксперту
            V[j] += P[i][j] * S[i]

    # 3. Создаем таблицу результатов
    df = pd.DataFrame({
        "Альтернатива": alternatives,
        "Вес V_j": V,
        "Ранг": range(1, 5)
    })

    # Сортируем по убыванию веса
    df = df.sort_values("Вес V_j", ascending=False)
    df["Ранг"] = range(1, 5)

    # 4. Выводим результаты
    st.subheader("📊 Результаты расчетов")

    col1, col2 = st.columns(2)

    with col1:
        st.markdown("**Нормированные коэффициенты компетентности:**")
        for i, s in enumerate(S, 1):
            st.write(f"S{i} = {s:.3f}")

    with col2:
        st.markdown("**Итоговые веса альтернатив:**")
        for i, (alt, weight) in enumerate(zip(alternatives, V), 1):
            st.write(f"V{i} = {weight:.3f}")

    st.markdown("---")
    st.subheader("🎯 Ранжирование альтернатив")

    # Красивая таблица с результатами
    st.dataframe(df.set_index("Ранг"), use_container_width=True)

    # Визуализация
    st.bar_chart(df.set_index("Альтернатива")["Вес V_j"])

    # Лучшая альтернатива
    best_alternative = df.iloc[0]["Альтернатива"]
    st.success(f"**Наиболее предпочтительная альтернатива:** {best_alternative}")